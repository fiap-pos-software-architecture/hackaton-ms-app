import path from "path";

export const Constantes = {
  FFMPEG_PATH: path.resolve("./bin/ffmpeg"), // Caminho para o executável ffmpeg
  FFPROBE_PATH: path.resolve("./bin/ffprobe"), // Caminho para o executável ffprobe
};
